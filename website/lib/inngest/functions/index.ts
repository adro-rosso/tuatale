/**
 * Barrel for the /api/inngest route handler to import all functions
 * in one line. Add new functions here as they land in later cycles
 * (Cycle A.5: customer notification email, Cycle A.6+: pipeline
 * variants).
 */
export { runPipelineJob } from './run-pipeline-job';
